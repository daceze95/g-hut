import GameGrid from '../components/GameGrid';
import Layout from '../components/Layout/Layout';

const Home = () => {
  return (
    <Layout>
      <GameGrid/>
    </Layout>
  );
};

export default Home;
